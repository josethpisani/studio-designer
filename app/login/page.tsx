import {login} from "../actions"; export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <div className="grid min-h-screen place-items-center bg-ink p-5"><form action={login} className="card w-full max-w-sm p-7"><p className="text-xs font-bold uppercase tracking-[.2em] text-coral">STUDIO DESIGNER</p><h1 className="mt-2 text-2xl font-black">Acceso a configuración</h1><p className="mt-2 text-sm text-slate-500">Ingresa tus credenciales para continuar.</p>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">Usuario o contraseña incorrectos.</p>}<label className="label mt-6">Usuario</label><input name="user" required className="field mb-4"/><label className="label">Contraseña</label><input name="password" type="password" required className="field"/><button className="btn mt-6 w-full bg-ink text-white">Iniciar sesión</button></form></div>}
