import Link from "next/link"; import { Plus,Search,ArrowUpRight } from "lucide-react"; import { query } from "../../lib/queries";
export default async function Clients(){const rows=await query<any>("SELECT c.*, COUNT(a.id) activity_count, SUM(CASE WHEN a.status NOT IN ('completed','cancelled') THEN 1 ELSE 0 END) pending_count, MAX(a.updated_at) last_activity FROM clients c LEFT JOIN activities a ON a.client_id=c.id GROUP BY c.id ORDER BY c.name"); return <div className="min-h-screen p-5 sm:p-8"><header className="mb-8 flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-coral">Directorio</p><h1 className="mt-2 text-3xl font-black">Clientes</h1><p className="mt-2 text-sm text-slate-500">Tus relaciones creativas, en un solo lugar.</p></div><Link href="/clientes/nuevo" className="btn bg-ink text-white"><Plus size={17}/> <span className="hidden sm:inline">Nuevo cliente</span><span className="sm:hidden">Nuevo</span></Link></header><div className="card overflow-hidden"><div className="border-b border-slate-100 p-4"><div className="flex max-w-sm items-center gap-2 rounded-xl bg-slate-50 px-3 py-2 text-sm text-slate-400"><Search size={16}/><span>Buscar clientes...</span></div></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-400"><tr><th className="px-5 py-3">Cliente</th><th className="px-5 py-3">Contacto</th><th className="px-5 py-3">Trabajos</th><th className="px-5 py-3">Pendientes</th><th></th></tr></thead><tbody className="divide-y divide-slate-100">{rows.map((c:any)=><tr key={c.id} className="hover:bg-slate-50"><td className="px-5 py-4"><Link href={`/clientes/${c.id}`} className="font-bold hover:text-coral">{c.name}</Link><div className="text-xs text-slate-400">{c.company||"Particular"}</div></td><td className="px-5 py-4 text-slate-500">{c.email||c.phone||"—"}</td><td className="px-5 py-4 font-semibold">{c.activity_count}</td><td className="px-5 py-4">{c.pending_count||0}</td><td className="px-5 py-4"><Link href={`/clientes/${c.id}`} className="text-slate-400 hover:text-ink"><ArrowUpRight size={18}/></Link></td></tr>)}</tbody></table>{!rows.length&&<p className="p-12 text-center text-sm text-slate-400">Crea tu primer cliente para empezar.</p>}</div></div></div>}
