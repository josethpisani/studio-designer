import Link from "next/link"; import { notFound } from "next/navigation"; import { query } from "../../../../lib/queries"; import { ClientForm } from "../../../../components/forms";
export default async function EditClient({params}:{params:Promise<{id:string}>}){const {id}=await params; const c=(await query<any>("SELECT * FROM clients WHERE id=?",[id]))[0]; if(!c)notFound(); return <div className="min-h-screen p-5 sm:p-8"><Link href={`/clientes/${id}`} className="text-sm font-semibold text-slate-500">← Volver al cliente</Link><h1 className="mb-6 mt-6 text-3xl font-black">Editar cliente</h1><ClientForm client={c}/></div>}
