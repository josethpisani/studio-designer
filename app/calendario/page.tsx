import Link from "next/link"; import { query } from "../../lib/queries"; import { StatusBadge } from "../../components/badge"; export default async function Calendar(){const rows=await query<any>("SELECT a.*,c.name client_name FROM activities a JOIN clients c ON c.id=a.client_id ORDER BY date(a.due_date)"); const grouped=rows.reduce((o:any,a:any)=>{(o[a.due_date]??=[]).push(a);return o},{}); return <div className="min-h-screen p-5 sm:p-8"><header className="mb-8"><p className="text-xs font-bold uppercase tracking-[.2em] text-coral">Planificación</p><h1 className="mt-2 text-3xl font-black">Calendario</h1><p className="mt-2 text-sm text-slate-500">Tus entregas ordenadas por fecha.</p></header><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{Object.entries(grouped).map(([day,acts]:any)=><div className="card p-5" key={day}><p className="text-xs font-bold uppercase tracking-wider text-slate-400">{new Date(day+"T12:00:00").toLocaleDateString("es-CO",{weekday:"long",day:"numeric",month:"long"})}</p><div className="mt-4 space-y-2">{acts.map((a:any)=><Link href={`/actividades/${a.id}`} className="block rounded-xl border border-slate-100 p-3 hover:bg-slate-50" key={a.id}><p className="text-sm font-bold">{a.title}</p><p className="mt-1 text-xs text-slate-400">{a.client_name}</p><div className="mt-2"><StatusBadge status={a.status}/></div></Link>)}</div></div>)}{!rows.length&&<p className="text-sm text-slate-400">No hay actividades agendadas.</p>}</div></div>}
