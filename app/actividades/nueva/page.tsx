import Link from "next/link"; import {query} from "../../../lib/queries"; import {ActivityForm} from "../../../components/forms";
export default async function New(){const clients=await query<any>("SELECT id,name,company FROM clients ORDER BY name");const designers=await query<any>("SELECT id,name,role FROM designers ORDER BY name");return <div className="min-h-screen p-5 sm:p-8"><Link href="/actividades" className="text-sm font-semibold text-slate-500">← Volver a actividades</Link><h1 className="mb-6 mt-6 text-3xl font-black">Nueva actividad</h1>{clients.length?<ActivityForm clients={clients} designers={designers}/>:<div className="card p-8 text-center"><p className="text-slate-500">Primero necesitas registrar un cliente.</p><Link href="/clientes/nuevo" className="btn mt-4 bg-ink text-white">Crear cliente</Link></div>}</div>}
