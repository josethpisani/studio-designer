const months=["ene","feb","mar","abr","may","jun","jul","ago","sep","oct","nov","dic"];
// @ts-ignore: valores numéricos provenientes de filas LibSQL
export function DashboardCharts({monthly,activities,byClient}:{monthly:any[],activities:any,byClient:any[]}){const max=Math.max(1,...monthly.flatMap(m=>[Number(m.created),Number(m.completed),Number(m.in_progress)]));const total=Object.values(activities).reduce((a:any,b:any)=>a+Number(b),0)||1;const states=[{key:"completed",label:"Finalizados",color:"bg-emerald-400"},{key:"in_progress",label:"En proceso",color:"bg-blue-400"},{key:"review",label:"En revisión",color:"bg-violet-400"},{key:"pending",label:"Pendientes",color:"bg-amber-400"},{key:"cancelled",label:"Cancelados",color:"bg-slate-400"}];return <section className="mt-8 grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><div className="card p-6"><div className="flex items-start justify-between"><div><h2 className="font-bold">Ritmo de trabajo</h2><p className="mt-1 text-xs text-slate-500">Trabajos creados, en proceso y finalizados por mes</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold text-blue-700">Datos en vivo</span></div><div className="mt-8 flex h-52 items-end gap-2 sm:gap-4">{monthly.length?monthly.map(m=><div className="flex h-full flex-1 flex-col items-center justify-end gap-2" key={m.month}><div className="flex h-full w-full items-end justify-center gap-1"><div title={`Creados: ${m.created}`} className="w-1/3 rounded-t-md bg-blue-300" style={{height:`${Number(m.created)/max*100}%`}}/><div title={`En proceso: ${m.in_progress}`} className="w-1/3 rounded-t-md bg-blue-500" style={{height:`${Number(m.in_progress)/max*100}%`}}/><div title={`Finalizados: ${m.completed}`} className="w-1/3 rounded-t-md bg-blue-800" style={{height:`${Number(m.completed)/max*100}%`}}/></div><span className="text-[10px] font-semibold text-slate-400">{months[Number(m.month.slice(5))-1]}</span></div>):<p className="m-auto text-sm text-slate-400">Aún no hay datos mensuales.</p>}</div><div className="mt-5 flex flex-wrap gap-4 text-xs text-slate-500"><span>▪ Creados</span><span className="text-blue-500">▪ En proceso</span><span className="text-blue-900">▪ Finalizados</span></div></div><div className="card p-6"><h2 className="font-bold">Estado actual</h2><p className="mt-1 text-xs text-slate-500">Distribución de tus trabajos</p><div className="mt-6 space-y-4">{states.map(s=><div key={s.key}><div className="mb-1 flex justify-between text-xs"><span>{s.label}</span><b>{activities[s.key]||0}</b></div><div className="h-2 rounded-full bg-slate-100"><div className={`h-2 rounded-full ${s.color}`} style={{width:`${Number(activities[s.key]||0)/total*100}%`}}/></div></div>)}</div></div><div className="card p-6 xl:col-span-2"><h2 className="font-bold">Trabajos por cliente</h2><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{byClient.map((c:any,i)=><div key={c.name}><div className="mb-1 flex justify-between text-xs"><span className="truncate">{c.name}</span><b>{c.count}</b></div><div className="h-2 rounded-full bg-slate-100"><div className={`h-2 rounded-full ${["bg-blue-300","bg-blue-400","bg-blue-500","bg-blue-600","bg-blue-800"][i]}`} style={{width:`${Math.min(100,Number(c.count)/(Number(byClient[0]?.count)||1)*100)}%`}}/></div></div>)}</div></div></section>}
